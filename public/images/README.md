# /public/images

- `og-image.png` — social share preview image, 1200×630px. Referenced in
  `src/data/site.ts` (`ogImage`) and used by `src/app/layout.tsx` for Open
  Graph / Twitter card metadata. Add your own image here.
- `projects/` — project screenshots/covers (see the README inside).
- `profile/` — your profile photo (see the README inside).
