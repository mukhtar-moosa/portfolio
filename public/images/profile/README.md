# Profile photo

Drop your headshot here as `profile.jpg` (or update the path in
`src/data/site.ts` under `profileImage`).

To render it instead of the placeholder icon, swap the placeholder `<div>`
in `src/components/sections/About.tsx` for Next.js's `<Image />` component.
